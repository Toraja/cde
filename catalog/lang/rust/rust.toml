[tools]
rust = { version = 'latest', postinstall = 'mise run postinstall:rust' }
protoc = { version = 'latest' }                                          # protoc is required by rust-analyzer
sccache = { version = 'latest' }
cargo-binstall = { version = 'latest' }
cargo-release = { version = 'latest' }
cargo-dist = { version = 'latest' }
