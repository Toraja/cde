version: '3.9'
services:
  cde:
    # TODO enable to pick a version
    # image: php_cde:${PHP_VERSION}
    # build:
    #   context: ./php/ctx
    #   args:
    #     - PHP_VERSION
    #   labels:
    #     php.version: ${PHP_VERSION:?}
