services:
  test:
    image: cde/host-setup-test
    volumes:
      - type: bind
        source: ./
        target: /home/${USER:?}/wd/
      - type: bind
        source: /home/${USER:?}/.ssh
        target: /home/${USER:?}/.ssh
