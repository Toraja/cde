- name: Install and setup taskwarrior
  tags:
    - taskwarrior-core
  ansible.builtin.import_tasks: taskwarrior.yml

- name: Install and setup taskwarrior-tui
  tags:
    - taskwarrior-tui
  ansible.builtin.import_tasks: taskwarrior-tui.yml

- name: Install tasknote
  tags:
    - tasknote
  ansible.builtin.import_tasks: tasknote.yml
