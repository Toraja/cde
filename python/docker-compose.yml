version: '3.9'
services:
  cde:
    build:
      labels:
        python.version: latest
    # volumes:
    #   - type: volume
    #     source: rustpath
    #     target: /home/${USER}/rust
# volumes:
#   rustpath:
#     name: rustpath_${RUST_VERSION}
#     labels:
#       prune: false
